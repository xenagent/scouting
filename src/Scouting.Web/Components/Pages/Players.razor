@page "/players"
@inject IPlayerService PlayerService

<PageTitle>Oyuncular — Scout</PageTitle>
<HeadContent>
    <meta name="description" content="Onaylı futbol oyuncularını keşfet. Pozisyon, lig ve ülkeye göre filtrele." />
</HeadContent>

<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-10">

    <div class="mb-8">
        <h1 class="font-display text-4xl text-white mb-2">OYUNCULAR</h1>
        <p class="text-scout-400">Admin onaylı scouting verileri</p>
    </div>

    <div class="card mb-8">
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4">
            <div>
                <label class="label">Arama</label>
                <input type="text" @bind="_filter.Search" @bind:event="oninput" @onkeyup="OnEnter"
                       placeholder="Oyuncu veya takım..." class="input" />
            </div>
            <div>
                <label class="label">Pozisyon</label>
                <select @bind="_filter.Position" class="input">
                    <option value="">Tümü</option>
                    <option value="GK">GK - Kaleci</option>
                    <option value="CB">CB - Stoper</option>
                    <option value="LB">LB - Sol Bek</option>
                    <option value="RB">RB - Sağ Bek</option>
                    <option value="CDM">CDM - Def. Orta</option>
                    <option value="CM">CM - Merkez</option>
                    <option value="CAM">CAM - Of. Orta</option>
                    <option value="LW">LW - Sol Kanat</option>
                    <option value="RW">RW - Sağ Kanat</option>
                    <option value="ST">ST - Forvet</option>
                </select>
            </div>
            <div>
                <label class="label">Lig</label>
                <input type="text" @bind="_filter.League" placeholder="Süper Lig..." class="input" />
            </div>
            <div>
                <label class="label">Ülke</label>
                <input type="text" @bind="_filter.Country" placeholder="Türkiye..." class="input" />
            </div>
        </div>
        <div class="flex justify-end mt-4">
            <button @onclick="Search" class="btn-primary">Filtrele</button>
        </div>
    </div>

    @if (_loading)
    {
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-5">
            @for (int i = 0; i < 8; i++) { <div class="card h-56 animate-pulse bg-scout-800/50" /> }
        </div>
    }
    else if (_players?.Count == 0)
    {
        <div class="text-center py-24 text-scout-500">
            <p class="text-5xl mb-4">⚽</p>
            <p>Oyuncu bulunamadı</p>
        </div>
    }
    else
    {
        <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 xl:grid-cols-4 gap-5">
            @foreach (var p in _players!) { <PlayerCard Player="p" /> }
        </div>
        @if (_players!.Count == _filter.PageSize)
        {
            <div class="flex justify-center mt-10">
                <button @onclick="LoadMore" class="btn-secondary">Daha Fazla Yükle</button>
            </div>
        }
    }
</div>

@code {
    private PlayerFilter _filter = new();
    private List<PlayerListItemVm>? _players;
    private bool _loading = true;

    protected override async Task OnInitializedAsync() => await Search();

    private async Task Search()
    {
        _loading = true;
        _filter.Page = 1;
        _players = (await PlayerService.GetListAsync(_filter)).Data;
        _loading = false;
    }

    private async Task LoadMore()
    {
        _filter.Page++;
        var more = (await PlayerService.GetListAsync(_filter)).Data;
        _players ??= [];
        _players.AddRange(more);
    }

    private async Task OnEnter(KeyboardEventArgs e)
    {
        if (e.Key == "Enter") await Search();
    }
}
